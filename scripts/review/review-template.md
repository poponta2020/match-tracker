# Cross Review - レビュー依頼プロンプト

あなたはコードレビュー担当です。以下のPRの差分をレビューしてください。

## PR情報
- **PR**: {{PR_URL}}
- **ブランチ**: {{BRANCH}} → {{BASE_BRANCH}}
- **タイトル**: {{PR_TITLE}}

## レビュー観点

以下の全観点でレビューしてください。問題がない観点は「問題なし」と明記してください。

### 1. コードの正しさ
- ロジックにバグはないか
- エッジケース（null、空リスト、境界値）の考慮漏れはないか
- 既存機能へのデグレードリスクはないか

### 2. 設計・アーキテクチャ
- 既存のレイヤードアーキテクチャ（Controller → Service → Repository → Entity）に従っているか
- DTO変換は適切か（`fromEntity()` パターン）
- 責務の分離は適切か（Service層にビジネスロジック、Controller層は薄く）

### 3. セキュリティ
- SQLインジェクション、XSSなどOWASP Top 10の脆弱性はないか
- `@RequireRole` による認可は適切か
- 入力バリデーションは十分か

### 4. テストの十分さ
- 変更に対応するテストが追加/更新されているか
- 正常系・異常系のカバレッジは十分か
- テストが実装の詳細に依存しすぎていないか

### 5. その他
- 不要なコード（デバッグ用コード、コメントアウト）が残っていないか
- パフォーマンスに問題のあるクエリやN+1問題はないか

## リポジトリ情報

- **リポジトリルート**: `c:/Users/popon/match-tracker`
- **仕様書**: `docs/SPECIFICATION.md`
- **設計書**: `docs/DESIGN.md`
- **画面一覧**: `docs/SCREEN_LIST.md`

差分だけでは判断できない場合、上記パスから実ファイルを `Read` / `Grep` で直接確認してください。
特に以下のケースでは実ファイルの調査を推奨します：
- 変更されたメソッドの呼び出し元・呼び出し先の確認
- Entity/DTOのフィールド定義の確認
- 既存のバリデーションや認可設定との整合性確認

## 差分

```diff
{{DIFF}}
```

## 出力形式

以下の形式でレビュー結果を出力してください：

```
## レビュー結果サマリー
- **PR**: #{{PR_NUMBER}}（レビュー {{REVIEW_ROUND}}回目）
- 総合評価: [APPROVE / REQUEST_CHANGES / COMMENT]
- 重大な問題: X件
- 軽微な指摘: X件

## 指摘事項

### [CRITICAL / WARNING / INFO] 指摘タイトル
- **ファイル**: ファイルパス:行番号
- **問題**: 何が問題か
- **修正案**: 具体的にどう直すべきか

（指摘ごとに繰り返し）
```

## 出力先

レビュー結果は以下のファイルに書き込んでください：
`{{RESULT_FILE}}`

上記の出力形式に従い、レビュー結果をこのファイルに直接書き込んでください。
**また、ファイルへの書き込みに加えて、同じレビュー結果を会話上にもテキストとして出力してください。**
