---
status: draft            # draft | iterating | locked
slug: <slug>
target: <ルート or コンポーネントパス>
chosen_direction: null   # 確定した方向性（例 "A"）。確定まで null
round: 0                 # 調整ループの回数
---
# <画面名> デザイン仕様（design-spec）

> `/design-screen` が出力する確定仕様。実装は `/define-feature <slug>` または `/implement <slug>` で行う。
> このファイルは**見た目の確定形**を記述する。本番コードのファイル割りやテストは実装スキル側で詰める。

## 1. 対象と狙い
- **対象画面:** <ルート / コンポーネント（例 `karuta-tracker-ui/src/pages/...`）>
- **現状の不満:** <見づらい点・動機>
- **狙い（ゴール）:** <この画面で達成したいこと>
- **主ユーザー / 主な使い方:** <眺める / 下調べ / 自分の確認 等>

## 2. 採用した方向性
- **方向性:** <A/B/C 等＋一言要約>
- **不採用案と理由:** <なぜ他案を採らなかったか（後日の蒸し返し防止）>
- **確定モック:** Claude Design `<画面名> (Redesign)` グループ / `preview/<slug>-<dir>.html`

## 3. レイアウト構成（上→下）
<セクションを上から順に。各セクションの中身・情報の優先順位・余白感>
1. …
2. …

## 4. 使用コンポーネント
- **既存コンポーネント:** PageHeader / PlayerChip / FilterBottomSheet / Layout / … （`karuta-tracker-ui/src/components/`）＋ lucide-react アイコン
- **新規が必要なもの:** <あれば。折りたたみ行・集計バー・月ヘッダ 等。挙動と見た目>

## 5. 状態（state）
- **通常:** …
- **空（データ無し）:** …
- **長大（多数の項目）:** <畳み方・初期表示件数・もっと見る 等>
- **エラー / 不明値:** <日付不明・順位なし 等の扱い>

## 6. 必要データ
- **表示フィールド:** <フィールド名と出所（`src/api/*.js` / バックエンド DTO）>
- **集計・導出:** <勝率・月別・回数などの算出方法と元データ。データ品質の注意>

## 7. インタラクション / レスポンシブ
- **操作:** <折りたたみ展開・フィルタ・タップ遷移 等>
- **モバイル:** 390px 基準。<横スクロール回避・タップ領域 等>

## 8. ガードレール準拠メモ
- 配色（gray ニュートラル＋赤primary／緑success／青info・link／黄warning）/ フォント（システム sans・数値は font-mono）/ 絵文字無し（lucide アイコン）/ 日付・時刻表記 — 逸脱があれば理由を明記。primary 主色は対象画面の既存実装に合わせる。

## 9. 残課題・実装への申し送り
- <未確定点・実装時に判断が要る点・パフォーマンス懸念 等>

## 10. 要件への宿題（→ /define-feature <slug>）
> 視覚だけで決められないロジック/遷移/データの論点をここに溜め、`/define-feature <slug>` で解決する（解決後この欄を空にする）。**収束ゲート＝この欄が空**。requirements.md 側の「デザインへの宿題」と対になる。
- <例: 相手名タップ時の遷移先・未解決相手の扱い・同名処理 など>
