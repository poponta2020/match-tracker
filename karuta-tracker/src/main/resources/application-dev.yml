spring:
  application:
    name: karuta-tracker

  # データソース設定（ローカル開発環境 + Docker MySQL）
  datasource:
    url: jdbc:mysql://localhost:3306/karuta_tracker?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Tokyo
    username: karuta
    password: karuta123
    driver-class-name: com.mysql.cj.jdbc.Driver

    # HikariCP接続プール設定
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 300000
      connection-timeout: 20000

  # JPA/Hibernate設定
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        jdbc:
          time_zone: Asia/Tokyo

  # 開発時のホットリロード
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

# ログ設定
logging:
  level:
    root: INFO
    com.karuta.matchtracker: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# Actuator設定
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
